# config.py

# A unique name for your application to store credentials under in the OS keychain
SERVICE_NAME = "com.yourcompany.braze-transifex-sync"

# The name of the local JSON file for storing non-sensitive settings
CONFIG_FILE_NAME = "config.json"
